apiVersion: v1
kind: Pod
metadata:
  name: nginx-configured
  namespace: ai-platform
spec:
  containers:
    - name: nginx
      image: nginx:1.27.0
      envFrom:
        - configMapRef:
            name: nginx-config